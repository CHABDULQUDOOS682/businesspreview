<%# locals: pagy %>
<% if pagy.pages > 1 %>
  <div class="mt-4 flex flex-col gap-3 border-t border-sand-200 pt-4 sm:flex-row sm:items-center sm:justify-between">
    <p class="text-sm text-sand-500">
      <%== pagy_info(pagy) %>
    </p>
    <div class="flex justify-end">
      <%== pagy_nav(pagy) %>
    </div>
  </div>
<% end %>
